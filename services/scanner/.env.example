# Go Scanner Environment Variables

PORT=3001
GIN_MODE=debug

# Git configuration
GIT_TIMEOUT=300
MAX_REPO_SIZE_MB=500

# Scanning configuration
MAX_CONCURRENT_SCANS=10
SCAN_TIMEOUT_SECONDS=600

# Gateway callback (to notify scan completion)
GATEWAY_URL=http://gateway:8000
